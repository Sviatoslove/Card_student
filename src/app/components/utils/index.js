import { getCardFromLocalStorage, setCardToLocalStorage } from './localStorage'
import { validator, validatorConfig } from './validator'
import { createCardStudent, declensionWord } from './settings'

export {
  getCardFromLocalStorage,
  setCardToLocalStorage,
  validator,
  createCardStudent,
  declensionWord,
  validatorConfig
}
